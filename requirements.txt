# Pinned to minimum versions known to ship prebuilt wheels for Python 3.13,
# so pip installs from wheels instead of trying to compile from source.
# If your machine is on Python 3.11/3.12, these versions work fine too.

numpy>=2.1.2
scikit-learn>=1.5.2
pandas>=2.2.3
matplotlib>=3.9.2
seaborn>=0.13.2
joblib>=1.4.2

SpeechRecognition>=3.10.4
librosa>=0.10.2
noisereduce>=3.0.3
soundfile>=0.12.1

selenium>=4.25.0
pywhatkit>=5.4
pyttsx3>=2.98
python-dotenv>=1.0.1

spacy>=3.8.2
sentence-transformers>=3.2.0

pvporcupine>=3.0.2
pyaudio>=0.2.14

# after installing, run:
# python -m spacy download en_core_web_sm
